services:
  nam-protocol-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nam-engine
    ports:
      - "7860:7860"
    environment:
      - GRADIO_SERVER_NAME=0.0.0.0
      - GRADIO_SERVER_PORT=7860
    restart: unless-stopped
    