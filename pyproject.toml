[project]
name = "nam-protocol-engine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.26.0",
    "pydantic>=2.13.5",
    "pypdf>=6.17.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
